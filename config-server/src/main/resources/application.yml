spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://192.168.40.60/root/hxjk-config.git
          username: root
          password: hxjk2018
#          default-label: ${spring.profiles.active}
          search-paths: 'src/main/resources/{application}'
          force-pull: true
server:
  port: 1201